# KitBerg.gg

## Running the code

Run `cd KitBerg_GG` to install the dependencies.

Run `npm run dev` to start the development server.
  

To build it for website

Run `npm run build` to build for Netlify

Then drag and drop the newly made 'dist' folder onto upload